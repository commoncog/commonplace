{{#foreach posts}}

<article class="{{post_class}} post">
		<header class="post-header">
			<h2 class="post-header__title"><a class="post-header__title-link" href="{{url}}">{{{title}}}</a></h2>
			<span class="post-header__meta">{{author}} {{tags prefix=(t "on ") separator=", "}} &middot; <time datetime="{{date format='YYYY-MM-DD'}}">{{date format="DD MMM YYYY"}}</time></span>
			<div class="clear"></div>
		</header>

		<section class="post__excerpt">
			<p>{{excerpt}}&hellip;</p>
		</section>
</article>

{{/foreach}}

{{pagination}}

