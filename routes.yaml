routes:
  /signup/: members/signup
  /login/: members/login
  /account/: members/account

collections:
  /:
    permalink: /{slug}/
    template:
      - index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
